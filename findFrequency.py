# input = "kkwcccddee"

def findrequency(string):
    i = 0 
    final_string = ""
    # reach to letters of string
    while i < len(string):

        c = string[i]
        j += 1
        # counter
        compressed = [1,c]
        while j < len(string):
            if str
    # look next index of letter
    # merge into one string
